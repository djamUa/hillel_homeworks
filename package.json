{
  "type": "module",
  "dependencies": {
    "log4js": "^6.9.1",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "cypress": "^13.2.0",
    "fs-extra": "^11.1.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:open": "cypress open",
    "cy:run_crome": " cypress run --headless -b chrome",
    "cy_run_chrome_headed": "cypress run --headed -b chrome"
  }
}
